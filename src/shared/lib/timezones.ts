// Россия не переводит часы с 2014 года, поэтому смещение региона постоянно.
// Числа из справочника достаточно: отдельная база IANA-зон здесь ничего не уточнит.
const REGIONS_BY_MSK_OFFSET: Record<string, string[]> = {
  '-1': ['Калининградская область'],
  '0': [
    'Адыгея', 'Дагестан', 'ДНР', 'Донецкая Народная Республика', 'Ингушетия',
    'Кабардино-Балкария', 'Кабардино-Балкарская Республика', 'Калмыкия',
    'Карачаево-Черкесия', 'Карачаево-Черкесская Республика', 'Карелия', 'Коми', 'Крым',
    'ЛНР', 'Луганская Народная Республика', 'Марий Эл', 'Мордовия',
    'Северная Осетия', 'Северная Осетия — Алания', 'Татарстан', 'Чечня', 'Чеченская Республика',
    'Чувашия', 'Чувашская Республика', 'Краснодарский край', 'Ставропольский край',
    'Архангельская область', 'Белгородская область', 'Брянская область', 'Владимирская область',
    'Волгоградская область', 'Вологодская область', 'Воронежская область', 'Запорожская область',
    'Ивановская область', 'Калужская область', 'Кировская область', 'Костромская область',
    'Курская область', 'Ленинградская область', 'Липецкая область', 'Московская область',
    'Мурманская область', 'Нижегородская область', 'Новгородская область', 'Орловская область',
    'Пензенская область', 'Псковская область', 'Ростовская область', 'Рязанская область',
    'Смоленская область', 'Тамбовская область', 'Тверская область', 'Тульская область',
    'Херсонская область', 'Ярославская область',
    'Москва', 'Санкт-Петербург', 'Севастополь', 'Ненецкий АО',
  ],
  '1': ['Удмуртия', 'Удмуртская Республика', 'Астраханская область', 'Самарская область', 'Саратовская область', 'Ульяновская область'],
  '2': [
    'Башкортостан', 'Башкирия', 'Пермский край', 'Курганская область', 'Оренбургская область',
    'Свердловская область', 'Тюменская область', 'Челябинская область',
    'ХМАО', 'ХМАО — Югра', 'Ханты-Мансийский автономный округ — Югра',
    'ЯНАО', 'Ямало-Ненецкий автономный округ',
  ],
  '3': ['Омская область'],
  '4': [
    'Республика Алтай', 'Тыва', 'Тува', 'Хакасия', 'Алтайский край', 'Красноярский край',
    'Кемеровская область', 'Кемеровская область — Кузбасс', 'Новосибирская область', 'Томская область',
  ],
  '5': ['Бурятия', 'Иркутская область'],
  '6': ['Забайкальский край', 'Амурская область', 'Якутия', 'Саха (Якутия)', 'Республика Саха'],
  '7': ['Приморский край', 'Хабаровский край', 'Еврейская АО', 'Еврейская автономная область'],
  '8': ['Магаданская область', 'Сахалинская область'],
  '9': ['Камчатский край', 'Чукотский АО', 'Чукотский автономный округ'],
};

// Якутия разбита на три пояса по районам, а района в наших данных нет.
// Показываем якутское время (основная часть республики) и предупреждаем об этом.
const MULTI_ZONE_REGIONS = ['Якутия', 'Саха (Якутия)', 'Республика Саха'];

// Тип субъекта в названии ни на что не влияет: «Воронежская область» и «Воронежская» —
// один и тот же ключ. Так справочник переживает разное оформление в файлах партнёров.
const NOISE_WORDS = new Set(['область', 'обл', 'республика', 'респ', 'край', 'автономный', 'округ', 'город', 'г', 'ао']);

function normalize(value: string): string {
  return value.toLowerCase().replace(/ё/g, 'е')
    .split(/[^a-zа-я0-9]+/)
    .filter((word) => word !== '' && !NOISE_WORDS.has(word))
    .join(' ');
}

const OFFSET_BY_REGION = new Map<string, number>();
for (const [offset, regions] of Object.entries(REGIONS_BY_MSK_OFFSET)) {
  for (const region of regions) OFFSET_BY_REGION.set(normalize(region), Number(offset));
}
const MULTI_ZONE = new Set(MULTI_ZONE_REGIONS.map(normalize));

const HOUR_MS = 3_600_000;
const MSK_UTC_OFFSET = 3;
const WORK_START_HOUR = 9;
const WORK_END_HOUR = 20;

type Workday = 'open' | 'before' | 'after';

interface RegionTime {
  offsetLabel: string;
  time: string;
  workday: Workday;
  multiZone: boolean;
}

// Граница рабочего дня называется прямо: «звонить неудобно» читалось как намёк
// на разницу часовых поясов, хотя дело в часе суток.
const WORKDAY_HINTS: Record<Workday, string> = {
  open: '',
  before: `, рабочий день начнётся в ${WORK_START_HOUR}:00`,
  after: `, рабочий день закончился в ${WORK_END_HOUR}:00`,
};

export function describeRegionTime(region: string, now: number): RegionTime | null {
  const key = normalize(region);
  const offset = OFFSET_BY_REGION.get(key);
  // Регион не из справочника — молчим. Показать чужое время хуже, чем не показать ничего.
  if (offset === undefined) return null;
  const local = new Date(now + (MSK_UTC_OFFSET + offset) * HOUR_MS);
  const hours = local.getUTCHours();
  return {
    offsetLabel: offset === 0 ? 'МСК' : `МСК${offset > 0 ? '+' : '−'}${Math.abs(offset)}`,
    time: `${String(hours).padStart(2, '0')}:${String(local.getUTCMinutes()).padStart(2, '0')}`,
    workday: hours < WORK_START_HOUR ? 'before' : hours >= WORK_END_HOUR ? 'after' : 'open',
    multiZone: MULTI_ZONE.has(key),
  };
}

/** Короткая пометка рядом со временем: смысл нельзя передавать одним цветом. */
export function regionTimeMark(value: RegionTime): string {
  return value.workday === 'open' ? '' : ', нерабочее время';
}

export function regionTimeHint(value: RegionTime): string {
  const base = `Местное время ${value.time}${WORKDAY_HINTS[value.workday]}`;
  return value.multiZone ? `${base}. В регионе несколько часовых поясов` : base;
}
